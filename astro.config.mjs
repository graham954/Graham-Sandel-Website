// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default) — deploys cleanly to Cloudflare Pages.
// Build command: `npm run build` · Output directory: `dist`
export default defineConfig({
  site: 'https://grahamsandel.com',
});
